{
  "description": "Non-blocking Stop-hook nudge: on a feature-dev session (per the session sentinel) reminds the model to update the living spec docs/features/<slug>/spec.md before wrapping up. Advisory only — does not inspect git.",
  "event": "Stop",
  "script": "spec-reminder.mjs",
  "settingsFragment": {
    "hooks": {
      "Stop": [
        {
          "hooks": [
            {
              "type": "command",
              "command": "node \"$CLAUDE_PROJECT_DIR\"/.claude/hooks/spec-reminder/spec-reminder.mjs"
            }
          ]
        }
      ]
    }
  }
}
