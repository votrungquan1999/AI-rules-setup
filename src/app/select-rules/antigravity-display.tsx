"use client";

import { ContentTabs } from "src/components/content-tabs";

/**
 * Display component for Antigravity agent
 * Uses tabbed layout for skills, workflows, and rules
 */
export function AntigravityDisplay() {
	return <ContentTabs />;
}
